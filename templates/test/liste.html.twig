{% extends 'base.html.twig' %}

{% block title %}{{title}}{% endblock %}

{% block body %}

<div class="example-wrapper">
    <h1>Hello {{ title }}! ✅</h1>
<ul>
        {% for i,item in liste %}
            <li>{{item}}</li>
        {% endfor %}
</ul>
</div>
{% endblock %}
